classdef Bond < Instrument
    properties
        CouponRate
        MaturityDate
        % ... (other bond-specific properties)
    end

    methods
        % ... (constructor, getCurrentValue, other bond-specific methods)
    end
end
